<%= form_for(@consumer) do |f| %>
  <% if @consumer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consumer.errors.count, "error") %> prohibited this consumer from being saved:</h2>

      <ul>
      <% @consumer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= f.label :ip_address %><br>
    <%= f.text_field :ip_address, :value => request.remote_ip, :readonly => true %>
  </div>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
    <!-- Modification for Tinymce  -->
    <!-- f.text_area :email, :class => "tinymce", :rows => 40, :cols => 120  -->
  </div>
  <div class="field">
    <%= f.label :clave %><br>
    <%= f.password_field :clave %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
