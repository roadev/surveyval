<p id="notice"><%= notice %></p>

<h1>Listing Survey Questions</h1>

<table>
  <thead>
    <tr>
      <th>Survey question</th>
      <th>Content</th>
      <th>Survey</th>
      <th>Cardinal</th>
      <th>Type question</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @survey_questions.each do |survey_question| %>
      <tr>
        <td><%= survey_question.survey_question_id %></td>
        <td><%= survey_question.content %></td>
        <td><%= survey_question.survey %></td>
        <td><%= survey_question.cardinal %></td>
        <td><%= survey_question.type_question %></td>
        <td><%= link_to 'Show', survey_question %></td>
        <td><%= link_to 'Edit', edit_survey_question_path(survey_question) %></td>
        <td><%= link_to 'Destroy', survey_question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Survey question', new_survey_question_path %>
